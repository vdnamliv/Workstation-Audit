# ============================================
# VT-AUDIT DATABASE - ENVIRONMENT VARIABLES
# ============================================
# Copy file nay thanh .env: mv .env.example .env
# Password ben duoi la password TEST, doi khi deploy production!
# ============================================

# PostgreSQL superuser (dung de init database)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres123

# ============================================
# LUU Y: Password trong 01-init.sql phai KHOP voi cac file .env:
# - keycloak123  -> 03-admin-api/.env (KC_DB_PASSWORD)
# - stepca123    -> 04-agent-api/.env (STEPCA_DB_PASSWORD)
# - vtapp123     -> 03-admin-api/.env va 04-agent-api/.env (DB_PASS)
# ============================================
