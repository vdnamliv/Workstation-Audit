# ============================================
# VT-AUDIT AGENT API - ENVIRONMENT VARIABLES
# ============================================
# Copy file nay thanh .env: mv .env.example .env
# Password ben duoi la password TEST, doi khi deploy production!
# ============================================

# --- DATABASE CONNECTION ---
# Production: doi thanh IP database server (10.211.130.51)
# Neu database cung server: dung localhost
DB_HOST=10.211.130.51
DB_USER=vt_app
DB_PASS=vtapp123
DB_NAME=vt_db

# --- STEPCA CONFIGURATION ---
STEPCA_NAME=VT-Audit-CA
# StepCA cung server: dung localhost
# Production: doi thanh IPs (10.211.130.44,10.211.130.47,10.211.130.48)
STEPCA_DNS_NAMES=localhost,127.0.0.1
STEPCA_PROVISIONER=vt-audit-provisioner
STEPCA_PASSWORD=stepca123
STEPCA_PROVISIONER_PASSWORD=stepca123

# --- STEPCA DATABASE (Optional - hien dung file-based) ---
# Password nay phai KHOP voi 01-init.sql (CREATE USER stepca)
STEPCA_DB_PASSWORD=stepca123

# --- AGENT BOOTSTRAP ---
# Token de agent enroll lan dau (phai du dai va random cho production)
AGENT_BOOTSTRAP_TOKEN=bootstrap-token-for-testing-only-change-in-production
