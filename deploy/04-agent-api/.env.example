# ============================================
# VT-AUDIT AGENT API SERVER - ENVIRONMENT
# Server: 10.211.130.47 (Primary), 10.211.130.48 (Secondary)
# ============================================
# Copy file nay thanh .env va thay doi cac gia tri

# ============================================
# DATABASE CONNECTION
# ============================================
# Ket noi vao VIP cua DB Server
DB_HOST=10.211.130.51
DB_USER=vt_app
DB_PASS=CHANGE_ME_MUST_MATCH_01_INIT_SQL
DB_NAME=vt_db

# ============================================
# STEPCA INITIALIZATION
# ============================================
# Cac gia tri nay dung de khoi tao StepCA lan dau
STEPCA_NAME=VT-Audit-CA
STEPCA_DNS_NAMES=10.211.130.44,10.211.130.47,10.211.130.48,localhost,stepca
STEPCA_PROVISIONER=vt-audit-provisioner
STEPCA_PASSWORD=CHANGE_ME_StepCA_Init_Password!
STEPCA_PROVISIONER_PASSWORD=CHANGE_ME_StepCA_Init_Password!

# ============================================
# AGENT BOOTSTRAP TOKEN
# ============================================
# Token de agent enroll lan dau (shared secret)
# Generate voi: openssl rand -base64 32
AGENT_BOOTSTRAP_TOKEN=CHANGE_ME_GENERATE_WITH_OPENSSL
