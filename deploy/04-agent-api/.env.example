# VT-AUDIT Agent API Configuration
# Copy this file to .env and fill in production values

# ============================================
# DATABASE CONNECTION
# ============================================
# Production: Use actual database server IP
DB_HOST=10.211.130.51
DB_USER=vt_app
DB_PASS=CHANGE_ME_VT_APP_PASSWORD
DB_NAME=vt_db

# ============================================
# STEPCA CONFIGURATION
# ============================================
# StepCA Database Password
STEPCA_DB_PASSWORD=CHANGE_ME_STEPCA_DB_PASSWORD

# StepCA Initialization
STEPCA_NAME=VT-Audit-CA
STEPCA_DNS_NAMES=10.211.130.44,10.211.130.47,10.211.130.48,gateway.vt-audit.local
STEPCA_PROVISIONER=vt-audit-provisioner
STEPCA_PASSWORD=CHANGE_ME_STEPCA_ADMIN_PASSWORD
STEPCA_PROVISIONER_PASSWORD=CHANGE_ME_STEPCA_ADMIN_PASSWORD

# ============================================
# AGENT BOOTSTRAP
# ============================================
# Token for agents to enroll
# Generate with: openssl rand -base64 32
AGENT_BOOTSTRAP_TOKEN=GENERATE_RANDOM_TOKEN_HERE
