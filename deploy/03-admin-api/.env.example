# ============================================
# VT-AUDIT ADMIN API - ENVIRONMENT VARIABLES
# ============================================
# Copy file nay thanh .env: mv .env.example .env
# Password ben duoi la password TEST, doi khi deploy production!
# ============================================

# --- DATABASE CONNECTION ---
# Production: doi thanh IP database server (10.211.130.51)
# Neu database cung server: dung localhost
DB_HOST=10.211.130.51
DB_USER=vt_app
DB_PASS=vtapp123
DB_NAME=vt_db

# --- KEYCLOAK DATABASE ---
# Password nay phai KHOP voi 01-init.sql (CREATE USER keycloak)
KC_DB_PASSWORD=keycloak123

# --- KEYCLOAK ADMIN ---
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin123

# --- OIDC CONFIG ---
# Keycloak trong cung compose file: dung service name keycloak:8080
# Production: doi thanh VIP (https://10.211.130.44:8443/realms/vt-audit)
OIDC_ISSUER_URL=http://localhost:8080/realms/vt-audit
OIDC_CLIENT_ID=vt-dashboard
OIDC_CLIENT_SECRET=vt-dashboard-secret-123

# --- STEPCA URL ---
# StepCA o server khac (.47/.48): dung VIP hoac IP
# Production: doi thanh VIP (https://10.211.130.44:443/step-ca)
STEPCA_URL=https://10.211.130.44:443/step-ca
