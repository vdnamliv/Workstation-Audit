# .env for Reverse Proxy VMs (.45 and .46)
# Copy this file to each proxy VM as .env in the project directory

# OIDC Configuration
OIDC_CLIENT_ID=vt-audit-client
OIDC_CLIENT_SECRET=your-client-secret-here
OIDC_COOKIE_SECRET=RandomBase64CookieSecret32Chars=

# Note: Proxy VMs don't need database or StepCA configs
# They only need OIDC credentials for oauth2-proxy
