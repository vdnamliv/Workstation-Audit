# --- CẤU HÌNH MÔI TRƯỜNG (Chỉ cần sửa ở đây khi sang Prod) ---
DOMAIN_NAME=gateway.local
PUBLIC_PORT=8443
# Production thì để: https, Local dev self-signed thì để: http hoặc https
PROTOCOL=https

# --- PostgreSQL ---
POSTGRES_USER=audit
POSTGRES_PASSWORD=ChangeMe123!
POSTGRES_DB=audit
# Dùng tên service 'postgres' thay vì localhost để các container gọi nhau
POSTGRES_DSN=postgres://audit:ChangeMe123!@postgres:5432/audit?sslmode=disable

# --- Keycloak ---
KEYCLOAK_DB=audit
KEYCLOAK_DB_USER=keycloak
KEYCLOAK_DB_PASSWORD=ChangeMe123!
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=pODFPavc1Kee6XiUKyuOryD6GkyaIkZQ
KEYCLOAK_REALM=vt-audit

# --- OAuth2-Proxy ---
OIDC_CLIENT_ID=dashboard-proxy
OIDC_CLIENT_SECRET=dashboard-secret
OIDC_COOKIE_SECRET=Q7YH8J5K1N4P6S9V
# Role admin cần check
OIDC_ADMIN_ROLE=admin

# --- Step-CA ---
STEPCA_NAME=VT-Audit-CA
# Tự động thêm domain vào list DNS
STEPCA_DNS_NAMES=stepca,localhost,gateway.local,agent-gateway.local
STEPCA_PROVISIONER=bootstrap@vt-audit
STEPCA_PASSWORD=ChangeMe123!
STEPCA_PROVISIONER_PASSWORD=ChangeMe123!
STEPCA_KEY_PATH=/home/step/secrets/bootstrap@vt-audit_key

# --- Agent ---
MTLS_CERT_TTL=24h
AGENT_BOOTSTRAP_TOKEN=123456